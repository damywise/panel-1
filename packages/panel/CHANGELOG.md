## 0.1.0

- Initial release: dockable / tabbed / splittable panel framework with a
  pluggable windowing backend (detaching is a no-op until a backend such as
  `package:panel_macos` is supplied). Platform-generic and web-capable.
